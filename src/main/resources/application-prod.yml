spring:
    datasource:
        url: jdbc:mysql://host.docker.internal/ogloszex?useSSL=false&allowPublicKeyRetrieval=true
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: tajnehaslo
    liquibase:
        contexts: prod